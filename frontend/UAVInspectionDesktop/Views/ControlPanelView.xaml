<UserControl x:Class="UAVInspectionDesktop.Views.ControlPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Border Style="{StaticResource PanelCardStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="34" />
                <RowDefinition Height="*" />
                <RowDefinition Height="24" />
            </Grid.RowDefinitions>

            <TextBlock Text="处理面板" Style="{StaticResource SectionTitleTextStyle}" />

            <UniformGrid Grid.Row="1" Columns="2" Rows="2" Margin="0,16,0,0">
                <Button Command="{Binding TogglePauseCommand}" Style="{StaticResource ActionButtonStyle}" Margin="0,0,8,8">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="{Binding PauseButtonText}" FontWeight="SemiBold" HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center" />
                        <TextBlock Text="{Binding RefreshModeText}" Style="{StaticResource CaptionTextStyle}" HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap" Margin="0,6,0,0" />
                    </StackPanel>
                </Button>
                <Button Command="{Binding RefreshCommand}" Style="{StaticResource PrimaryActionButtonStyle}" Margin="8,0,0,8">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="立即刷新" FontWeight="SemiBold" HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center" />
                        <TextBlock Text="同步最新接口数据" Style="{StaticResource CaptionTextStyle}" HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap" Margin="0,6,0,0" />
                    </StackPanel>
                </Button>
                <Button Command="{Binding ToggleOverlayCommand}" Style="{StaticResource ActionButtonStyle}" Margin="0,8,8,0">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="检测叠加" FontWeight="SemiBold" HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center" />
                        <TextBlock Text="{Binding OverlayButtonText}" Style="{StaticResource CaptionTextStyle}" HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap" Margin="0,6,0,0" />
                    </StackPanel>
                </Button>
                <Button Command="{Binding ToggleDisplayModeCommand}" Style="{StaticResource WarningActionButtonStyle}" Margin="8,8,0,0">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="画面模式" FontWeight="SemiBold" HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center" />
                        <TextBlock Text="{Binding DisplayModeText}" Style="{StaticResource CaptionTextStyle}" HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap" Margin="0,6,0,0" />
                    </StackPanel>
                </Button>
            </UniformGrid>

            <TextBlock Grid.Row="2" Text="当前操作将作用于主监控画面、告警列表和实时叠加层。" Style="{StaticResource CaptionTextStyle}" Margin="0,14,0,0" />
        </Grid>
    </Border>
</UserControl>
