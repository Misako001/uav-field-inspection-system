<UserControl x:Class="UAVInspectionDesktop.Views.QuickAccessPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Border Style="{StaticResource PanelCardStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid>
                <TextBlock Text="快捷入口" Style="{StaticResource SectionTitleTextStyle}" />
                <TextBlock Text="功能操作与详情切换" HorizontalAlignment="Right" Style="{StaticResource CaptionTextStyle}" VerticalAlignment="Center" />
            </Grid>

            <ItemsControl Grid.Row="1"
                          Margin="0,16,0,0"
                          ItemsSource="{Binding QuickAccessItems}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="2" Rows="3" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Style="{StaticResource QuickAccessCardButtonStyle}"
                                Command="{Binding DataContext.SelectQuickAccessCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding}"
                                Margin="0,0,12,12">
                            <Border Style="{StaticResource InsetCardStyle}"
                                    Padding="16">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="*" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Grid>
                                        <Border Width="44" Height="44" Background="{Binding AccentColor}" CornerRadius="12" HorizontalAlignment="Left">
                                            <TextBlock Text="{Binding IconGlyph}" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#FFFFFF" FontSize="18" FontWeight="Bold" />
                                        </Border>
                                        <Border HorizontalAlignment="Right"
                                                VerticalAlignment="Top"
                                                Background="#0E1823"
                                                BorderBrush="{StaticResource BorderBrush}"
                                                BorderThickness="1"
                                                CornerRadius="6"
                                                Padding="8,3">
                                            <TextBlock Text="{Binding Badge}" FontSize="10" Foreground="{StaticResource TextSecondaryBrush}" />
                                        </Border>
                                    </Grid>
                                    <StackPanel Grid.Row="1" VerticalAlignment="Center" Margin="0,14,0,10">
                                        <TextBlock Text="{Binding Title}"
                                                   FontSize="14"
                                                   FontWeight="SemiBold"
                                                   Foreground="{StaticResource TextPrimaryBrush}"
                                                   TextWrapping="Wrap"
                                                   MaxHeight="38" />
                                        <TextBlock Text="{Binding Subtitle}"
                                                   FontSize="11"
                                                   Margin="0,4,0,0"
                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                   TextWrapping="NoWrap"
                                                   TextTrimming="CharacterEllipsis" />
                                    </StackPanel>
                                    <DockPanel Grid.Row="2" LastChildFill="False">
                                        <TextBlock Text="查看详情"
                                                   FontSize="11"
                                                   Foreground="{Binding AccentColor}"
                                                   VerticalAlignment="Center" />
                                        <TextBlock Text="›"
                                                   FontSize="15"
                                                   Margin="6,-1,0,0"
                                                   Foreground="{Binding AccentColor}"
                                                   VerticalAlignment="Center" />
                                    </DockPanel>
                                </Grid>
                            </Border>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </Border>
</UserControl>
