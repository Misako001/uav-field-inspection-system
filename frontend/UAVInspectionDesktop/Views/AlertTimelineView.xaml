<UserControl x:Class="UAVInspectionDesktop.Views.AlertTimelineView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Border Style="{StaticResource PanelCardStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="42" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="告警时间轴" Style="{StaticResource SectionTitleTextStyle}" />
                    <Border Style="{StaticResource TagBorderStyle}" Background="#123125" BorderBrush="{StaticResource SuccessBrush}" Margin="12,0,0,0">
                        <TextBlock Text="{Binding VisibleAlertCount}" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource SuccessBrush}" />
                    </Border>
                    <TextBlock Text="{Binding ActiveAlertFilter, StringFormat=当前筛选：{0}}" Style="{StaticResource CaptionTextStyle}" Margin="10,0,0,0" VerticalAlignment="Center" />
                </StackPanel>

                <ItemsControl ItemsSource="{Binding AlertFilters}" HorizontalAlignment="Right">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Content="{Binding Label}"
                                    Tag="{Binding IsActive}"
                                    Command="{Binding DataContext.FilterAlertsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding Key}"
                                    Style="{StaticResource FilterChipButtonStyle}"
                                    Margin="0,0,10,0" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="288" />
                </Grid.ColumnDefinitions>

                <ListBox ItemsSource="{Binding Alerts}"
                         SelectedItem="{Binding SelectedAlert, Mode=TwoWay}"
                         Background="Transparent"
                         BorderThickness="0"
                         ItemContainerStyle="{StaticResource SelectableCardListBoxItemStyle}"
                         ScrollViewer.HorizontalScrollBarVisibility="Auto"
                         ScrollViewer.VerticalScrollBarVisibility="Disabled">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Width="232" Height="126" Margin="0,0,12,0" Background="#111922" BorderBrush="{Binding AccentColor}" BorderThickness="2" CornerRadius="10" Padding="12">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="22" />
                                        <RowDefinition Height="22" />
                                        <RowDefinition Height="*" />
                                        <RowDefinition Height="20" />
                                    </Grid.RowDefinitions>
                                    <Grid>
                                        <TextBlock Text="{Binding OccurredAt, StringFormat=HH:mm:ss}" Style="{StaticResource CaptionTextStyle}" />
                                        <Border HorizontalAlignment="Right" Background="#0E171F" BorderBrush="{Binding AccentColor}" BorderThickness="1" CornerRadius="4" Padding="7,2">
                                            <TextBlock Text="{Binding Severity}" FontSize="10" FontWeight="SemiBold" Foreground="{Binding AccentColor}" />
                                        </Border>
                                    </Grid>
                                    <TextBlock Grid.Row="1" Text="{Binding AlertType}" Style="{StaticResource CardTitleTextStyle}" />
                                    <TextBlock Grid.Row="2" Text="{Binding Content}" Style="{StaticResource BodyTextStyle}" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap" />
                                    <Grid Grid.Row="3">
                                        <TextBlock Text="{Binding Status}" Style="{StaticResource CaptionTextStyle}" />
                                        <TextBlock Text="{Binding Confidence, StringFormat=P0}" HorizontalAlignment="Right" FontFamily="Bahnschrift SemiBold" FontSize="11" Foreground="{StaticResource SuccessBrush}" />
                                    </Grid>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <Border Grid.Column="1" Style="{StaticResource InsetCardStyle}" Margin="16,0,0,0">
                    <StackPanel>
                        <TextBlock Text="选中告警" Style="{StaticResource CardTitleTextStyle}" />
                        <TextBlock Text="{Binding SelectedAlert.AlertType, TargetNullValue=暂无选中告警}" FontSize="18" FontWeight="SemiBold" Margin="0,12,0,0" />
                        <TextBlock Text="{Binding SelectedAlert.Content, TargetNullValue=请选择左侧告警查看详情。}" Style="{StaticResource BodyTextStyle}" Margin="0,10,0,0" TextWrapping="Wrap" Foreground="{StaticResource TextSecondaryBrush}" />
                        <TextBlock Text="{Binding SelectedAlert.Status, StringFormat=状态：{0}, TargetNullValue=状态：无}" Style="{StaticResource CaptionTextStyle}" Margin="0,12,0,0" />
                        <TextBlock Text="{Binding SelectedAlert.Confidence, StringFormat=置信度：{0:P0}, TargetNullValue=置信度：无}" Style="{StaticResource CaptionTextStyle}" Margin="0,4,0,0" />
                        <UniformGrid Columns="2" Margin="0,16,0,0">
                            <Button Content="处理选中" Command="{Binding MarkSelectedAlertHandledCommand}" Style="{StaticResource PrimaryActionButtonStyle}" Margin="0,0,8,0" />
                            <Button Content="清理已处理" Command="{Binding ClearHandledAlertsCommand}" Style="{StaticResource DangerActionButtonStyle}" Margin="8,0,0,0" />
                        </UniformGrid>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </Border>
</UserControl>
