# First deployment can point to the server public IP.
# After the domain is approved and HTTPS is enabled, switch these values to:
# VITE_API_BASE_URL=https://example.cn
# VITE_WS_BASE_URL=wss://example.cn
VITE_API_BASE_URL=http://SERVER_PUBLIC_IP
VITE_WS_BASE_URL=ws://SERVER_PUBLIC_IP
